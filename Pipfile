[[source]]
name = "aliyun"
url = "https://mirrors.aliyun.com/pypi/simple"
verify_ssl = true

[dev-packages]
black = {version = "==21.12b0", markers = "python_version == '3.9'"}
isort = {version = ">=5.9.3", markers = "python_version == '3.9'"}
pipenv = {version = ">=2018.11.26", markers = "python_version == '3.9'"}
secretstorage = {version = ">=3", markers = "sys_platform == 'linux'"}

[packages]
fire = ">=0.2.1"

[requires]
python_version = "3"
